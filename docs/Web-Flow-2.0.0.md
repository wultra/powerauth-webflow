# Migration from 1.10.0 to 2.0.0

This guide contains instructions for migration from PowerAuth WebFlow version `1.10.x` to version `2.0.0`.


## Java Package and Maven GroupId Changes

Maven groupId and java package have changed from `io.getlime.*` to `com.wultra.*`.
